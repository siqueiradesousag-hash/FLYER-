{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "PORT=3000 BASE_PATH=/ VITE_BASE_PATH=/ pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run build",
    "typecheck:libs": "echo \"Typecheck desativado para build rapido\"",
    "typecheck": "echo \"Typecheck desativado para build rapido\"",
    "cap": "cap",
    "cap:sync": "cap add android || echo \"Plataforma ja adicionada\"; cap sync android"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}